{
  "name": "fer-sitio",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-contenido.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.18.2"
  }
}
